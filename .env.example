# Stellar Testnet endpoints (same for every project in the fleet)
NEXT_PUBLIC_HORIZON_URL=https://horizon-testnet.stellar.org
NEXT_PUBLIC_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
NEXT_PUBLIC_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Per-project contract IDs (populated by scripts/deploy.sh)
NEXT_PUBLIC_MAIN_CONTRACT_ID=
# Optional: only set when SPEC.md has uses_custom_token: true
NEXT_PUBLIC_TOKEN_CONTRACT_ID=
