export function HeroSection() {
  return (
    <section className="mt-6">
      <p className="text-base leading-relaxed text-muted sm:text-lg">
        A SEP-41 token sale on Stellar Testnet.{" "}
        <span className="text-fg">Pledge XLM during the window</span>; if the
        soft cap is met, claim tokens pro-rata; if not, refund. Hard cap caps
        the raise; deadline closes the sale.
      </p>
    </section>
  );
}
